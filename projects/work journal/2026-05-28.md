---

---
- Pairing with Fajar benerin migration
TODO
- Poluted health check
- More brand-specific extraction logic. Right now we only have one extraction flow shared across all brands, but we want to make it more deterministic (schema/example-output based, avoiding overfitting, etc.)
- rewrite /airline/search

---
```mermaid
flowchart TD
    A([Server Starts]) --> ML & J

    ML["Check for users with connected accounts\n(every 1 hour)"] -->|repeat| ML

    J["Random delay before first sync\n(0–60 min, spreads server load)"] --> S
    S["Wait if too many syncs running\n(max 20 at once)"] --> C

    subgraph task ["Per-User Background Sync"]
        C["Fetch today's purchase emails from Gmail"]
        --> N["Skip emails already processed"]
        --> D["Classify emails as purchases or not"]
        --> E["Extract order details"]
        --> F["Save new purchases to database"]
        C -->|"Account disconnected or token revoked"| X(["Stop sync for this user"])
    end

    F -->|"Success — wait 1 hour"| S
    task -->|"Error — retry with backoff\n(5 min → 10 min → ... → 60 min max)"| S
```

