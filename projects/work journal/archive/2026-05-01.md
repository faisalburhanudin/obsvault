- fix conflict & bump
- TODO
	- fix PR issue
		- Test:
			- is it reconfigure when client not from US ?
			- is it reconfigure when client using domains youtube.com ? (massive)
			- is it reconfigure over and over again on request machine
	- scan token

---
Current condition:
- A machine will not be configured if total_machines == 1.
- total_machines == 1 when we configure using provision_one.

Problem:
- The machine being configured may have different proxy requirements from the current configuration.

---
Prev design
- configure happen on backfill + get_app (unnasign & already assigned), 
- configure happen on backfill + get_app (state unassign), since the num of machine always be one (pre-configure) best-of-n (configure) is unneded on get_app that assigned


get_app: always hit configure, configure is skip if machines == 1


Proposal
- configure == "always configure"
- let caller do the logic

Impact
- backfill always configure
- get_app:
	- configure on unnasign app
	- skip configure on assigned app

configure will do
- check headers & default
