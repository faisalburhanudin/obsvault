- fix remotebrowser GCE
	- http://100.78.42.88:23456
	- pointing MCP to chromefleet
	- [ ] Make live view usable by using tls from tailscale
- [ ] fix flyfleet
	- [ ] 
- [ ] test lambda
- [ ] benchmark
---
- Update oauth
	- github demos
- GCE to Fly via wireguard
	- install wireguard

---
```
sudo podman run -d --rm --network getgather-net -p 23456:23456 \
     --env-file ~/mcp.env --name mcp-getgather mcp-getgather:latest
```
```
sudo podman run -d --rm --network getgather-net -p 8300:8300 \
     --env-file /opt/chromefleet.env --cap-add=NET_ADMIN --cap-add=NET_RAW \
     --device=/dev/net/tun:/dev/net/tun -v /run/podman.sock:/run/podman.sock \
     --user root --name chromefleet chromefleet:latest
```
```
CHROMEFLEET_PROXY_URL=REDACTED
CHROMEFLEET_URL=http://chromefleet:8300
```